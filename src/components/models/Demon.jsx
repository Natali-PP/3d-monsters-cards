/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.5 public/models/Demon.gltf -o src/components/models/Demon.jsx -r public/
*/

import React, { useRef, useState, useEffect } from 'react'
import { useGLTF, useAnimations, useCursor } from '@react-three/drei'

export function Demon(props) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('/models/Demon.gltf')
  const { actions } = useAnimations(animations, group);
  const [hovered, setHovered] = useState(false)

  useCursor(hovered);

  useEffect(() => {
    actions['Idle'].reset().fadeIn(0.5).play();
    return () => actions['Idle'].fadeOut(0.5)
  }, [])

  useEffect(() => {
    if (hovered) {
      actions['Dance'].reset().fadeIn(0.5).play();
      return () => actions['Dance'].fadeOut(0.5)
    }
  }, [hovered])

  return (
    <group ref={group} {...props} dispose={null} onPointerOver={() => setHovered(true)} onPointerLeave={() => setHovered(false)}>
      <group name="Scene">
        <group name="MonsterArmature">
          <primitive object={nodes.Body} />
          <primitive object={nodes.Head} />
          <skinnedMesh name="Demon001" geometry={nodes.Demon001.geometry} material={materials.Texture} skeleton={nodes.Demon001.skeleton} />
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/models/Demon.gltf')
