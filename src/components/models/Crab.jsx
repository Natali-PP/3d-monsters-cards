/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.2.5 public/models/Crab.gltf -o src/components/models/Crab.jsx -r public/
*/

import React, { useEffect, useRef, useState } from 'react'
import { useGLTF, useAnimations } from '@react-three/drei'

export function Crab(props) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('/models/Crab.gltf')
  const { actions } = useAnimations(animations, group)
  const [hovered, setHovered] = useState(false)

  useEffect(() => {
    actions['Idle'].reset().fadeIn(0.5).play();
    return () => actions['Idle'].fadeOut(0.5)
  }, [])

  useEffect(() => {
    console.log('hovered', hovered)
    if (hovered) {
      actions['Dance'].reset().fadeIn(0.5).play();
      return () => actions['Dance'].fadeOut(0.5)
    }
  }, [hovered])
  return (
    <group ref={group} {...props} dispose={null} onPointerOver={() => setHovered(true)} onPointerLeave={() => setHovered(false)}>
      <group name="Scene">
        <group name="MonsterArmature">
          <primitive object={nodes.Body} />
          <primitive object={nodes.Head} />
          <skinnedMesh name="Crab" geometry={nodes.Crab.geometry} material={materials.Texture} skeleton={nodes.Crab.skeleton} />
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/models/Crab.gltf')
